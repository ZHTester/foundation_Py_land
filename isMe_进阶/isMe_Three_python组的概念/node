1 str list tuple 都统称为序列 ：序列很多操作是十分相似的
  序列共有的操作 为什么称之为序列因为每种类型的数据都是有顺序的不是无序的
  1 str list tuple 取值都是可以通过游标 通过切片的方式来获取到的
    序列中的每个元素都会被分配成一个序号 - 序号
  2 str list tuple 切片取值 -  切片中含有三个值，其中就是代表步长
  3 序列都是可以使用+ * 加号和乘号
  4 序列都是可以判断 变量是否存在于序列中或者是否不在序列中 in not in
  5 序列都是可以计算长度的len()
  6 序列中找到最大的元素 max() min() 求最小的值
  7 ord() 函数就是我们要转换成accic码的方法

2 总结
数字:
    1 整形 int
    2 浮点类型 float
    3 布尔类型 bool
    4 复数 complex


组   序列  可以通过下标索引来访问 也可以通过切片来访问
     1 字符串 str  不可变的数据类型
     2 列表  list 可变的数据类型
     3 元组 tuple 可变的数据类型

     无序没有索引 不能切片 不能通过游标来访问
     set集合:
     key:value 键值对是最基本的概念
     dict 字典
