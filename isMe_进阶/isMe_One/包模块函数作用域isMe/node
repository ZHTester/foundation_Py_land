会写代码是很简单的，但是写出优良的代码是不容易的

1 封装性 高性能 复用性  ~ 读原代码 抽象能力
2 编程就是现实生活与与代码之间的一个抽象的能力

------python的项目组织结构---
一个 包  --- 包含多个模块 --- 包含多个类
                   ---- 函数 和 变量 (这个又单存的属于类下面的特性不单单是属于 包和模块下面的)

------包和模块的名称   包和模块来组织项目
1 不同包，两个同名的模块 包名称.模块名称(这样就形成了一个路径，这个路径就叫做命名空间)   ---- 这样就叫命名空间
2 如果一个文件夹成文一个包那么这个文件夹下就必须有__init__.py这个文件
3 如果文件包没有这个文件__init__.py 那么python就认为这是一个普通的文件夹而不是一个文件包
4 __init_.py 其实他就是一个模块，什么都不用来标注
5 __init__.py 这个文件的模块名称不需要 命名空间的标注，其实就是包名的本身
6 真是的项目就是多个包和多个模块的相互调用构成的项目
7  pyc 文件就是编译器自己编译的二维码文件
8 我们不能再import下导入模块下的变量的 逗号分隔
9 import 只能导入模块
10 from import 其中 import会引入 包名 模块名称  变量 函数
12 换号是末尾 用\ 来换行，当然也可以使用括号来进行换行
13

(init下的使用*************)
11 __init__
  1 当我们导入模块下的时候，一个包下的模块我们就会先执行这个_init_模块文件
  2



